# Source Code

NodeFF implementation source lives here after the first Build Spec + Sprint are approved.

Current status: **HOLD**.

Cursor must not add product implementation while `build-spec/CURRENT.json` has `implementation_enabled=false`.
