# Findings — Quarantine

Build 過程的異常先進這裡，不得直接改產品 contract。

Classification：
IMPLEMENTATION_BUG / TEST_BUG / SPEC_AMBIGUITY / DESIGN_DELTA_CANDIDATE / BUILD_BLOCKER。

每筆必須包含 Expected / Actual / Evidence / Attempts / affected baseline + sprint + task。
